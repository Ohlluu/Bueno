import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bueno - A Coffee Experience Company | Chicago",
  description: "Experience coffee differently at Bueno. A coffee experience company bringing artisanal vibes to Chicago. Visit us at 2901 W Cermak Rd.",
  keywords: "bueno coffee, coffee experience, chicago coffee, artisanal coffee, bueno days, cermak coffee",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
